   <header>
        <div id="MenuBtn" class="fas fa-bars"></div>
        <a href="#" class="logo"><span>G</span>.ParroT</a>

        <nav class="navbar">
            <a href="{{ path('home.index') }}">Home</a>
            <a href="{{ path('products.index.public') }}">Vehicules</a>
            <a href="{{ path('arrivages.index.public') }}">Nos Arrivages</a>
            <a href="./#Service">Services</a>
            <a href="{{ path('review.index.public') }}">Reviews</a>
            <a href="./#Contact">Contact</a>
        </nav>
       
        {% if app.user and is_granted('ROLE_ADMIN') %}
        <div class="dropdown mx-4">
            <button class="btn dropdown-toggle m-0" type="button" data-bs-toggle="dropdown" aria-expanded="true">
                Action
            </button>
            <ul class="dropdown-menu Action fs-5">
                <li><a class="dropdown-item" href="{{ path('products') }}">Nos Produits !</a></li>
                <li><a class="dropdown-item" href="{{ path('products.new') }}">Nouveau Produit !</a></li>
                <li><a class="dropdown-item" href="{{ path('arrivages.index') }}">Les arrivages !</a></li>
                <li><a class="dropdown-item" href="{{ path('arrivages.new') }}">Nouveau Arrivage</a></li>
                <li><a class="dropdown-item" href="{{ path('user.index') }}">Liste des utilisateurs</a></li>
                <li><a class="dropdown-item" href="{{ path('review') }}">Liste des avis</a></li>
            </ul>
        </div>
        {% endif %}
       

        {% if app.user %}
        <div class="dropdown">
            <button class="btn dropdown-toggle m-0" type="button" data-bs-toggle="dropdown" aria-expanded="true">
                {{ app.user.fullName}}
            </button>
            <ul class="dropdown-menu Action fs-5">
                <li><a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Modifier Identity</a></li>
                <li><a class="dropdown-item" href="{{ path('user.edit.password', {id : app.user.id}) }}">Modifier Password</a></li>
                <li><a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a></li>
            </ul>
        </div>
        {% else %}

        <div id="LoginBtn">
            <button class="btn">SignUp</button>
            <i class="fas fa-user"></i>
        </div>
        {% endif %}
    </header>